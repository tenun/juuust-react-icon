import React from 'react';
import styled from 'styled-components';

const Header = styled.header`
  display: block;
  text-align: center;
`;

export default () => (
  <Header>
    <h1>Tenun Icons</h1>
  </Header>
);
